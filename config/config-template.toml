PreserveThreading=true
[mattermost]
    [mattermost.betagouvwebhook]
    Server="${MATTERMOST_BETAGOUV_SERVER}"
    Team="${MATTERMOST_BETAGOUV_TEAM}"
    PrefixMessagesWithNick=true
    RemoteNickFormat="<{NICK}> "
    PreserveThreading=true
    UseUserName=true
    WebhookURL="https://${MATTERMOST_BETAGOUV_WEBHOOK}"
    WebhookBindAddress="${MATTERMOST_ALUMNI_WEBHOOK}"
    [mattermost.alumniwebhook]
    Server="${MATTERMOST_BETAGOUV_SERVER}"
    Team="${MATTERMOST_BETAGOUV_TEAM}"
    PrefixMessagesWithNick=true
    RemoteNickFormat="<{NICK}> "
    PreserveThreading=true
    UseUserName=true
    WebhookURL="https://${MATTERMOST_ALUMNI_WEBHOOK}"
    WebhookBindAddress="${MATTERMOST_BETAGOUV_WEBHOOK}"
    [mattermost.betagouv]
    Server="${MATTERMOST_BETAGOUV_SERVER}"
    Team="${MATTERMOST_BETAGOUV_TEAM}"
    PrefixMessagesWithNick=true
    RemoteNickFormat="<{NICK}> "
    PreserveThreading=true
    Token="${MATTERMOST_BETAGOUV_TOKEN}"
    UseUserName=true
    [mattermost.alumni]
    Server="${MATTERMOST_BETAGOUV_SERVER}"
    Team="${MATTERMOST_BETAGOUV_ALUMNI_TEAM}"
    PrefixMessagesWithNick=true
    RemoteNickFormat="<{NICK}> "
    PreserveThreading=true
    Token="${MATTERMOST_ALUMNI_TOKEN}"
    UseUserName=true
[slack]
    [slack.betagouv]
    Token="${SLACK_BETAGOUV_TOKEN}"
    PrefixMessagesWithNick=true
    RemoteNickFormat="[On parle ici -> https://mattermost.incubateur.net et de l'aide sur #aide-mattermost] <{NICK}> "

[[samechannelgateway]]
    enable = true
    name = "samechannel1"
    accounts = [ "mattermost.betagouv","mattermost.alumni" ]
    channels = [ "tmp-lucas-test" ]
